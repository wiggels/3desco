# release-plz config. https://release-plz.dev/docs/config
#
# 3desco ships as a container image, not a crate. Publishing to crates.io is
# disabled via the per-package `publish = false` below; release-plz still
# manages the version bump, CHANGELOG, git tag, and GitHub release. The vX.Y.Z
# tag then drives the Docker publish workflow.

[workspace]
# not a library, so there is no public API to diff against a registry release
semver_check = false
# cut a git tag and a GitHub release on each version bump
git_tag_enable = true
git_release_enable = true
# human in the loop -- no auto merge of release PRs
pr_branch_prefix = "release-plz-"

# 3desco ships as a container image, not a crate. Disabling crates.io publishing
# MUST be done here, not with `publish = false` in Cargo.toml: release-plz treats
# a Cargo `publish = false` package as unmanaged and skips it entirely (no tag,
# no GitHub release). This keeps release-plz processing the package -- bumping
# the version, cutting the tag and GitHub release -- while never running
# `cargo publish`.
[[package]]
name = "three-desco"
release = true
publish = false
git_tag_enable = true
git_release_enable = true

[changelog]
header = """# 3desco Changelog\n
All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
"""
body = """
## [{{ version | trim_start_matches(pat="v") }}]\
    {%- if release_link -%}\
        ({{ release_link }})\
    {% endif %} - {{ timestamp | date(format="%Y-%m-%d") }}
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | upper_first }}
{% for commit in commits %}
- {% if commit.breaking %}[**breaking**] {% endif %}\
    {{ commit.message | upper_first }}\
    {% if commit.links %} ({% for link in commit.links %}[{{ link.text }}]({{ link.href }}){% if not loop.last %}, {% endif %}{% endfor %}){% endif %}
{%- endfor %}
{% endfor %}
"""
trim = true
commit_parsers = [
    { message = "^feat", group = "Added" },
    { message = "^add", group = "Added" },
    { message = "^fix", group = "Fixed" },
    { message = "^perf", group = "Performance" },
    { message = "^refactor", group = "Changed" },
    { message = "^style", group = "Changed" },
    { message = "^doc", group = "Documentation" },
    { message = "^test", group = "Tests" },
    { message = "^chore\\(deps\\)", group = "Dependencies" },
    { message = "^chore", skip = true },
    { message = "^ci", skip = true },
    { body = ".*security", group = "Security" },
]
